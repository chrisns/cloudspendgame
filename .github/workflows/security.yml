name: "Security Scanning"
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
jobs:
  scan:
    name: Security Scan
    uses: chrisns/.github/.github/workflows/security-scan.yml@b6ebe209e05782e84cae59778145a9bc6267bdaf # main
    permissions:
      security-events: write
      statuses: write
