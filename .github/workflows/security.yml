name: "Security Scanning"
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
jobs:
  scan:
    name: Security Scan
    uses: chrisns/.github/.github/workflows/security-scan.yml@7bf8cb0fcb31ebfac44cd9d8f41f049d8902f066 # main
    permissions:
      security-events: write
      statuses: write
