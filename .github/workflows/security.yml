name: "Security Scanning"
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
jobs:
  scan:
    name: Security Scan
    uses: chrisns/.github/.github/workflows/security-scan.yml@4769373d96052157e9b5b63d3ad833e0952d96b8 # main
    permissions:
      security-events: write
      statuses: write
