name: "Security Scanning"
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
jobs:
  scan:
    name: Security Scan
    uses: chrisns/.github/.github/workflows/security-scan.yml@8033c61b57f8568ec3739a83f720d2796cf39a83 # main
    permissions:
      security-events: write
      statuses: write
      contents: read
      actions: read
